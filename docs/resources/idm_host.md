---
subcategory: "idm_host"
---

# Add host in Redhat IDM 

```hcl
resource "idm_host" "my_host" {
    fqdn = 
    description =
    random = 
    userpassword =
    randompassword =
    force =
}
```